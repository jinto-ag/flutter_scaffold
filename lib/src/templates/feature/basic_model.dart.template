/// Basic model template.
class {{PASCAL_MODEL_NAME}}Model {
  const {{PASCAL_MODEL_NAME}}Model({
{{#HAS_FIELDS}}
{{FIELDS}}
{{/HAS_FIELDS}}
  });

{{#HAS_FIELDS}}
  // Properties
{{FIELDS}}

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is {{PASCAL_MODEL_NAME}}Model &&
          runtimeType == other.runtimeType;

  @override
  int get hashCode => Object.hash([{{FIELD_NAMES}}]);

  @override
  String toString() => '{{PASCAL_MODEL_NAME}}Model({{#HAS_FIELDS}}{{FIELD_NAMES}}{{/HAS_FIELDS}})';

  /// Create a copy with updated values
  {{PASCAL_MODEL_NAME}}Model copyWith({
{{FIELDS_WITH_OPTIONAL}}
  }) {
    return {{PASCAL_MODEL_NAME}}Model(
{{COPY_FIELDS}}
    );
  }

  /// Convert to Map for JSON serialization
  Map<String, dynamic> toJson() {
    return {
{{JSON_FIELDS}}
    };
  }

  /// Create from Map for JSON deserialization
  factory {{PASCAL_MODEL_NAME}}Model.fromJson(Map<String, dynamic> json) {
    return {{PASCAL_MODEL_NAME}}Model(
{{FROM_JSON_FIELDS}}
    );
  }
{{/HAS_FIELDS}}
{{^HAS_FIELDS}}
  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is {{PASCAL_MODEL_NAME}}Model &&
          runtimeType == other.runtimeType;

  @override
  int get hashCode => runtimeType.hashCode;

  @override
  String toString() => '{{PASCAL_MODEL_NAME}}Model()';

  /// Create a copy with updated values
  {{PASCAL_MODEL_NAME}}Model copyWith() {
    return const {{PASCAL_MODEL_NAME}}Model();
  }

  /// Convert to Map for JSON serialization
  Map<String, dynamic> toJson() => {};

  /// Create from Map for JSON deserialization
  factory {{PASCAL_MODEL_NAME}}Model.fromJson(Map<String, dynamic> json) {
    return const {{PASCAL_MODEL_NAME}}Model();
  }
{{/HAS_FIELDS}}
}