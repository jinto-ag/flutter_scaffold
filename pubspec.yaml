name: flutter_scaffold
description: A production-grade Flutter project scaffolding tool with Clean Architecture.
version: 0.1.0
repository: https://github.com/jinto-ag/flutter_scaffold

executables:
  flutter_scaffold:

environment:
  sdk: ^3.11.0-296.4.beta

# Add regular dependencies here.
dependencies:
  args: ^2.7.0
  mason_logger: ^0.3.3
  meta: ^1.17.0
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  flutter:
    sdk: flutter
